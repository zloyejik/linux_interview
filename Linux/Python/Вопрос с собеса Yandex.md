
```python
"""
Реализовать функцию, которая сокращает повторяющиеся подряд буквы в переданной строке до LN (L - буква, N - количество повторений) и возвращает результирующую строку

- Валидная строка на входе содержит буквы A-Z или пустая
- Если строка невалидна, то необходимо возвращать ошибку
- Если символ встречается один раз, то он остается неизменным

**Ограничения:**
- O(n) по памяти (рост памяти, выделенной при работе, должен быть пропорционален росту размера входящих данных)
- O(n) по времени (рост времени работы функции должен быть пропорционален росту размера входящих данных)

**Тестовые данные:**
- "AAAABBBCCXYZDDDDEEEFFFAAAAAABBBBBBBBBBBBBBBBBBBBBBBBBBBB" -> "A4B3C2XYZD4E3F3A6B28"
- "" -> ""
- "A" -> "A"
- " " -> error
"""

def rle(s: str) -> str:
    # your code here
```